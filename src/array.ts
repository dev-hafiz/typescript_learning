//* Basic Declaration:

let numbers: number[] = [1, 2, 3, 4, 5];
let fruits: string[] = ["apple", "banana", "cherry"];

//* Type Inference:
let inferredNumbers = [1, 2, 3]; // Inferred type: number[]

